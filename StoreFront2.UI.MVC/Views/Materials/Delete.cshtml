@model StoreFront2.DATA.EF.Material

@{
    ViewBag.Title = "Delete";
}

<h2>@ViewBag.Title</h2>

<h3>Are you sure you want to delete @Model.MaterialName?</h3>
<div>
    <h4>Material</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.MaterialName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MaterialName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete Permanaently" class="btn btn-danger pull-left" />
            @Html.ActionLink("Cancel", "Index", null, new {@class ="pull-right btn btn-dark" })
        </div>
    }
</div>
